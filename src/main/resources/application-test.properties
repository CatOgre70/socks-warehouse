spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.url=jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
spring.datasource.username=socksadmin
spring.datasource.password=MDJX7Sj46Hjk

spring.sql.init.mode=never
spring.h2.console.enabled=true

# To avoid issues with Empty JSON schemas
spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = create-drop

# Liquibase configuration file and scripts
spring.liquibase.change-log=classpath:liquibase/changelog-master-test.yaml
